# AI Provider
GROQ_API_KEY=gsk_your_actual_groq_api_key
# Future configuration
ENVIRONMENT=development

# Private feedback dashboard API
# Generate a long random secret and keep it server-side only.
ADMIN_FEEDBACK_TOKEN=replace_with_a_long_random_secret
# Optional; defaults to backend/data/arcsec_feedback.db
FEEDBACK_DB_PATH=
